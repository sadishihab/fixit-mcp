__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.env
.coverage
htmlcov/
.DS_Store

# Downloaded manual PDFs (fetched by scripts/fetch_manuals.py) -- never commit these.
data/manuals/pdf/

# Parsed manual chunks (derived from the PDFs above by scripts/parse_manuals.py).
data/manuals/parsed/

# Per-chunk extraction cache (scripts/extract_codes.py) -- derived, not the
# artifact. data/index/error_codes.json itself IS committed.
data/index/.extract_cache/

# Local SQLite persistence for household appliance state (dev/demo backend --
# see CLAUDE.md; production is intended to use AgentCore Memory instead).
data/state/
